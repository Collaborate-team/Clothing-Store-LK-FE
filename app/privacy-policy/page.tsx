import TermsCondition from "@/components/pages/TermsCondition";

export default function PrivacyPolicy() {
  return <TermsCondition />; // Reusing the layout for now, content can be customized later
}
